interface iCategoryCardProps {
    styles?: string
}

export function CategoryCard({ styles }: iCategoryCardProps) {
    return (
        <div className={`min-h-[400px] h-full bg-white shadow-md rounded mb-3 lg:mb-0 flex-1 flex`}>

        </div>
    )
}